
services:

  postgres-db:

    image: postgres

    environment:
      POSTGRES_DB: demo
      POSTGRES_USR: demo
      POSTGRES_PWD: demo

    volumes:
      -pgdata:/var/lib/postgressql/data

    ports:
      - "5432:5432"

volumes:
  pgdata: