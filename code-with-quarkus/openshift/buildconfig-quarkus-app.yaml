apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: quarkus-app
spec:
  source:
    type: Binary      # <-- binary build
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: quarkus-app:latest
